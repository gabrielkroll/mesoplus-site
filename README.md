# Meso+ marketing site

Public marketing site for [www.mesoplus.app](https://www.mesoplus.app).

## Local preview

```sh
python3 -m http.server 3002 --directory .
```

Open `http://localhost:3002`.

The site is intentionally a single `index.html` with no package manager, build step, or framework.
